class Table {
    constructor(id, pos, sockets, inGame){
         this.id = id;
         this.pos = pos;
         this.sockets = sockets;
         this.inGame = inGame;
    }
}

module.exports = Table;